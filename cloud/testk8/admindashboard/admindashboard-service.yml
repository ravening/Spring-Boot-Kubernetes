apiVersion: v1
kind: Service
metadata:
  name: admindashboard
  namespace: jhipster
  labels:
    app: admindashboard
spec:
  selector:
    app: admindashboard
  type: ClusterIP
  ports:
    - name: http
      port: 8080
