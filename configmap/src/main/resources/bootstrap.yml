spring:
  cloud:
    kubernetes:
      config:
        enabled: true
        sources:
          - namespace: ingress-nginx
            name: configmaps
      reload:
        enabled: true
        mode: event
        strategy: refresh